#include "main.h"
#include <string.h>

/**
 * _strlen - Entry point
 * Description: Function returns the length of a string
 * @s: String
 * Return: length of string
 */

int _strlen(char *s)
{
	return (strlen(s));
}
